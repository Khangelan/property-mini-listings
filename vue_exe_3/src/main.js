// main.js
import { createApp } from 'vue'
import App from './App.vue'

// Import global styles if you have them
import './style.css'

// Create the Vue app and mount it to #app in index.html
createApp(App).mount('#app')
